import Testing
@testable import IRIS

// IRIS v0.0.1 — test placeholder pour valider que la config Tuist + xcodebuild compile bien.
// v0.0.x suivantes : tests par module au fur et à mesure.

struct IRISTests {
    @Test func placeholder() {
        #expect(Bool(true))
    }
}
